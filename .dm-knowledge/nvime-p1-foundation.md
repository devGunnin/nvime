- **[pitfall]** Lua: 'cond and nil or fallback' always yields the fallback - never use the and/or idiom when the true-branch value can be nil or false (it silently turned every successful RPC response into an error)  _(2026-08-31T15:41:35Z)_
- **[pitfall]** vim.json.decode turns JSON null into vim.NIL (userdata, truthy) - decode sidecar frames with { luanil = { object = true, array = true } } or the first index of a null field crashes  _(2026-08-31T15:41:35Z)_
- **[invariant]** Claude Agent SDK 0.3.x: options.env REPLACES the subprocess environment (spread process.env yourself); pathToClaudeCodeExecutable accepts a native binary and any path not ending .js/.mjs/.ts is spawned directly - that is how nvime drives the local claude and its subscription login (system/init reports apiKeySource 'none')  _(2026-08-31T15:41:35Z)_
- **[command]** sidecar: npm --prefix agent install && npm --prefix agent run build (esbuild --packages=external, so node_modules must stay); tests: npm --prefix agent test, nvim --clean -l tests/run.lua, stylua --check lua plugin tests, luacheck lua plugin tests  _(2026-08-31T15:41:38Z)_
