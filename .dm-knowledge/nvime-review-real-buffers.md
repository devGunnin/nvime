- **[pitfall]** Neovim stores window-local options per (window, buffer) pair: nvim_win_set_buf drops what was set for the previous buffer (fillchars, winhighlight, statuscolumn...), so any window that swaps buffers must re-apply its chrome AFTER the swap, not once at creation  _(2026-09-02T11:27:22Z)_
- **[pitfall]** a bufhidden='wipe' scratch buffer cannot be swapped out of its window and back — it is wiped the moment it is hidden; a pane that alternates between a scratch render and a real file buffer needs bufhidden='hide' plus an explicit delete on every teardown path  _(2026-09-02T11:27:22Z)_
- **[pitfall]** headless nvim never redraws, so treesitter parses nothing lazily and get_captures_at_pos comes back empty even with an active highlighter — a probe must call parser:parse(true) first  _(2026-09-02T11:27:23Z)_
- **[command]** review-pane e2e (real CLI, ~2 min, costs tokens): tests/e2e/review-buffers.sh — builds a tiny lua change, opens the review tab, asserts the pane is the clone's own normal buffer with treesitter captures, marks not text, ]c/t round-trip, and no clone buffer left after close  _(2026-09-02T11:27:23Z)_
- **[pitfall]** nvim_buf_set_extmark's `strict = false` covers the COLUMN, not the line: an out-of-range row still raises `Invalid 'line': out of range`, so any row from patch arithmetic must be clamped by the caller before it reaches the API  _(2026-09-02T12:00:16Z)_
- **[pitfall]** nvim_buf_is_loaded is NOT "the user owns this buffer": `:badd`, an arglist (`nvim a.lua b.lua`) and every buffer of a restored `:mksession` but the current one are listed and unloaded. Ownership has to be read as `bufexists(path)` BEFORE `bufadd`, or the plugin unlists and wipes buffers the user created  _(2026-09-02T12:00:16Z)_
- **[pitfall]** `:checktime` is not a safe reload from a redraw — with `autoread` off it opens a W11 modal nothing can answer. `vim.cmd('edit')` inside `nvim_buf_call`, guarded on `vim.bo[buf].modified`, reloads without prompting  _(2026-09-02T12:00:16Z)_
- **[pitfall]** a `:split` copies the window-local option values of the window it splits from, so `winhighlight`/`fillchars` set on a plugin's pane follow the user into an unrelated window; `vim.wo[win][0]` does not prevent it — the new window has to be reset (WinNew + vim.schedule, since WinNew fires before it is entered)  _(2026-09-02T12:00:16Z)_
- **[pitfall]** `:edit`/`:edit!` re-reads a buffer and CLEARS `readonly` with it (`nomodifiable` survives), so a read-only lock silently degrades to a writable `:w` after any reload — hold it with a `BufReadPost` autocmd scoped to the buffers you own, not by setting the options once  _(2026-09-02T12:34:44Z)_
- **[pitfall]** a module-level 'one float at a time' latch must clear on WinClosed AND BufWipeout of the float, not only in its own key callbacks: `:q`, `:only` or a `tabclose` of the tab it lives in otherwise latches it for the whole nvim session, and every later ask is refused pointing at a float that is not on screen  _(2026-09-02T12:34:45Z)_
- **[pitfall]** caching a directory stat 'per capture' is not the same as caching a file stat: the clone root vanishing mid-session is exactly the case the check exists for, so keep the one `isdirectory` per draw live and cache only the per-file `filereadable`  _(2026-09-02T12:34:45Z)_
