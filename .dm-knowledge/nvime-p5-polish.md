- **[pitfall]** Lua: a top-level 'local agent = require(nvime.agent)' in a module reached transitively from require('nvime') (init.lua) can get permanently pinned to a spec file's fake package.loaded['nvime.agent'] if that module is first loaded while the fake is installed. Capture lazily (return require(...) inside a function) for anything reached through init.lua's eager top-level requires.  _(2026-09-01T00:49:12Z)_
- **[invariant]** Lua never shells out to git directly - that stays the sidecar's (TS) job. completion.lua's @-completion respects .gitignore via a hand-rolled matcher over vim.fs.dir, not 'git ls-files', to keep that boundary.  _(2026-09-01T00:49:12Z)_
- **[pitfall]** print() in a headless 'nvim --l script.lua' can silently coalesce two consecutive short lines onto one (confirmed on 0.12.3) even though the source list has them as separate elements - a smoke script verifying line-by-line output must read the real buffer (nvim_buf_get_lines) or write via io.stdout, never trust print() output framing.  _(2026-09-01T00:49:12Z)_
