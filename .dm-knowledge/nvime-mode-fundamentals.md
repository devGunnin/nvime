- **[invariant]** input-mode rules (all surfaces): a surface driven by normal-mode keys calls modes.normal() when it takes focus (nvim carries insert across window/tab switches, and every send leaves the prompt in insert; modes.normal covers i/R, ni* from i_CTRL-O, and select modes); every list buffer is nomodifiable; a prompt key reaches insert ONLY by an explicit `insert = true|'when-empty'` on its key entry - NEVER inferred from the key's shape, which swept in <C-n> and broke the panel's own completion popup; and a hint never names a key that does something else in the window showing it (normal-only keys are marked n_<key>)  _(2026-09-02T13:16:12Z)_
- **[pitfall]** headless 'nvim --clean -l' cannot HOLD insert mode across a Lua call - feedkeys 'x', nvim_input+vim.wait and startinsert all end insert when the typeahead empties, so mode assertions must stub vim.fn.mode/vim.cmd (see tests/lua/modes_spec.lua opened_from_insert) rather than typing their way into insert  _(2026-09-02T13:16:12Z)_
- **[pitfall]** every insert-mode mapping nvime adds to a box the reader types in is <expr> + pumvisible()-guarded: with the popup up the native key is returned so completion keeps working, and the action runs on vim.schedule (an <expr> mapping may not open a window). Same pattern in panel.lua (bind_prompt_insert) and compose.lua's insert <Esc>, which returns <C-e> to dismiss the popup instead of cancelling  _(2026-09-02T13:16:12Z)_
- **[invariant]** a big-change steer travels to a RUNNER over its control socket: big.steerable() needs session.runnerLive, or a recorded session.runner plus this editor following the run. An in-sidecar fallback build (NVIME_DETACHED=0, or a runner that could not spawn) records no runner, so s, the prompt steer path and the 'type to steer it' hint all decline it  _(2026-09-02T13:16:12Z)_
