- **[invariant]** Claude Agent SDK 0.3.x / claude 2.1.252 streaming input: a user message handed over mid-turn either gets FOLDED into the running turn (one result, no second turn ever) or runs as its own turn (a second result ~1s later) - and BOTH results report queued_turn_count: 0, so the field never distinguishes them. Trusting it closes the input stream between the two and drops the steer's turn; waiting for a second result hangs forever on a fold. Arm a grace timer that the next result cancels  _(2026-09-01T09:44:26Z)_
- **[pitfall]** A message already handed to the SDK's input iterator is in the CLI's stdin pipe, so ending the input stream flushes it rather than discarding it - 'delivered' is the honest boundary to record, and closing after it loses nothing  _(2026-09-01T09:44:26Z)_
- **[invariant]** sun_path is ~104 bytes, and a bind with a longer path truncates instead of erroring - a per-session control socket must live under XDG_RUNTIME_DIR (fallback /tmp/nvime-<uid>, 0700, lstat-checked for owner and non-symlink), keyed by a hash, never inside the deep <data>/nvime/big/<repo-slug>/<session>/ store dir  _(2026-09-01T09:44:26Z)_
- **[invariant]** Replay and live subscription must be established in ONE synchronous step with no await between them, and the backlog written BEFORE the socket joins the broadcast set - otherwise an attacher either misses an event appended in the gap or renders a live event ahead of the history it follows  _(2026-09-01T09:44:26Z)_
- **[pitfall]** A detached runner tearing down accepts a socket connection and then destroys it, so an attach that lands in that window gets nothing - every follow needs the append-only log as its backstop, replayed from the cursor it actually reached  _(2026-09-01T09:44:26Z)_
- **[invariant]** The record's single writer is whoever holds the run claim, so session.runner is written and cleared inside BigService's #run - and only that field is rewritten (re-read, mutate, save), because a run body re-reads and saves the record itself (merge, answer) and saving the object the run started with would revert it  _(2026-09-01T09:44:35Z)_
- **[pitfall]** A live lock cannot tell a detached runner from a second Neovim - both are foreign claims. 'build died' vs 'building elsewhere' needs BOTH the recorded runner pid AND the lock's pid to match; a recorded runner with a dead claim is what reads as died  _(2026-09-01T09:44:36Z)_
- **[command]** detached-build e2e (real CLI, ~5 min, costs tokens): tests/e2e/detached-build.sh - headless nvim A specs/approves/starts and exits, the shell watches events.ndjson grow, headless nvim B attaches, steers, and follows to the terminal event; NVIME_E2E_WORK pins the scratch dir  _(2026-09-01T09:44:36Z)_
