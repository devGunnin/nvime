- **[pitfall]** big.lua's on_event drops every event whose params.id is neither state.request_id nor state.attach_id - a run started from the review tab (threads.lua) sets neither, so a detached rebase streams into nothing; threads.lua subscribes on its own and matches the id it captured in on_sent  _(2026-09-01T13:10:13Z)_
- **[invariant]** BigStore.liveRunner needs BOTH a recorded session.runner and a live claim at that same pid, and the runner records itself only once BigService.#run starts - so for a few ms after a detached run claims the session it is indistinguishable from a second editor  _(2026-09-01T13:10:13Z)_
- **[command]** issue-10 e2e (real CLI, ~4 min, costs tokens): tests/e2e/rebase-merge.sh - builds a change, moves main under it, then drives the review tab's own M/R/M keymaps; asserts base-moved is refused, the R indicator actually spun, and main really ends at the merged commit  _(2026-09-01T13:14:26Z)_
