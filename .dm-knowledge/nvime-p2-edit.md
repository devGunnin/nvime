- **[pitfall]** Claude Agent SDK 0.3.x with permissionMode 'default': the CLI's own safe-command classifier auto-approves some Bash calls WITHOUT invoking canUseTool - a programmatic PreToolUse hook returning permissionDecision 'ask' is required to force every gated tool through the callback (hooks passed in Options are JS callbacks, so settingSources [] still holds)  _(2026-08-31T17:56:24Z)_
- **[pitfall]** Bare allowedTools entries auto-approve a tool before canUseTool runs (the SDK warns CLAUDE_SDK_CAN_USE_TOOL_SHADOWED) - never list a mutating tool there  _(2026-08-31T17:56:24Z)_
- **[invariant]** Reconciling a buffer with a file the agent rewrote needs 'silent noautocmd keepalt write!' after nvim_buf_set_lines: plain :write raises W12 and without the write nvim's stored mtime stays stale and the next :checktime raises W11  _(2026-08-31T17:56:24Z)_
- **[pitfall]** Neovim only syncs undo on the way back to the main loop, so two API edits in one tick share an undo block - a new run must break it explicitly with 'let &undolevels = &undolevels'; undojoin only joins  _(2026-08-31T17:56:24Z)_
- **[command]** e2e edit smoke: build the sidecar, then nvim --clean --headless -l <script> with vim.wait on edit.is_running(); the real claude CLI drives it end to end  _(2026-08-31T17:56:24Z)_
- **[pitfall]** path.resolve/path.join collapse `..` LEXICALLY before any symlink is followed, so `<root>/symlink-to-outside/../x` normalizes back inside the root while the kernel walks out of the link's target - any root-containment check must resolve one component at a time (realpathSync per accumulated prefix, `..` applied to the RESOLVED prefix)  _(2026-08-31)_
- **[invariant]** An approval frame must carry the raw payload it asks about, not only a clipped one-line summary - a truncated command is not something a human can consent to; nvime bounds it at 8KB with an explicit `truncated` flag the float renders loudly  _(2026-08-31)_
- **[pitfall]** `write!` suppresses W11/W12 AND the "file changed since you started editing" guard - before forcing the write, re-read disk and require it to still hold one of the change's two sides, or a concurrent external write is destroyed silently  _(2026-08-31)_
- **[pitfall]** One assistant message can carry several tool_use blocks on the SAME file, and their tool_results arrive together - snapshotting all of them up front makes every record claim the whole delta; settle the earlier tool at the later tool's canUseTool call, which is the only moment its true `after` is on disk  _(2026-08-31)_
- **[invariant]** nvim_buf_set_lines adjusts the cursor, so restoring a saved absolute lnum after an edit UNDOES that adjustment and teleports the user - anchor cursor and topline with extmarks in their own namespace and restore the rows the marks ended on  _(2026-08-31)_
- **[pitfall]** Buffer.toString('utf8') is lossy on invalid UTF-8 (latin-1 source files), so a text snapshot can stop describing the file's bytes - round-trip the decode and record it opaque when it does not survive  _(2026-08-31)_
