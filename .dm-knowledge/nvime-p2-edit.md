- **[pitfall]** Claude Agent SDK 0.3.x with permissionMode 'default': the CLI's own safe-command classifier auto-approves some Bash calls WITHOUT invoking canUseTool - a programmatic PreToolUse hook returning permissionDecision 'ask' is required to force every gated tool through the callback (hooks passed in Options are JS callbacks, so settingSources [] still holds)  _(2026-08-31T17:56:24Z)_
- **[pitfall]** Bare allowedTools entries auto-approve a tool before canUseTool runs (the SDK warns CLAUDE_SDK_CAN_USE_TOOL_SHADOWED) - never list a mutating tool there  _(2026-08-31T17:56:24Z)_
- **[invariant]** Reconciling a buffer with a file the agent rewrote needs 'silent noautocmd keepalt write!' after nvim_buf_set_lines: plain :write raises W12 and without the write nvim's stored mtime stays stale and the next :checktime raises W11  _(2026-08-31T17:56:24Z)_
- **[pitfall]** Neovim only syncs undo on the way back to the main loop, so two API edits in one tick share an undo block - a new run must break it explicitly with 'let &undolevels = &undolevels'; undojoin only joins  _(2026-08-31T17:56:24Z)_
- **[command]** e2e edit smoke: build the sidecar, then nvim --clean --headless -l <script> with vim.wait on edit.is_running(); the real claude CLI drives it end to end  _(2026-08-31T17:56:24Z)_
