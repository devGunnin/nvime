- **[invariant]** the debug log's redaction is a boundary, not a nicety: lua/nvime/log.lua and agent/src/debuglog.ts each hold their OWN copy of the secret-name rule, the content-key set and the 200-char clip, and both are asserted by name (log_spec/debuglog.test.ts). Changing one without the other silently un-redacts half the shared timeline — 'key' matches only as a whole word or a suffix, so 'keymaps' is not a secret  _(2026-09-02T15:12:44Z)_
- **[pitfall]** nvime.rpc's Client:_claim is the ONE place a request settles (reply, deadline, failed write) - it wraps the pending callback to log the outcome, so anything that settles a request without going through _claim (only _fail_all, which logs for itself) leaves an unmatched 'rpc >' line in the debug log  _(2026-09-02T15:12:44Z)_
- **[pitfall]** agent.ensure must NOT send debug.set when the log is off - a fresh sidecar is already off, and the extra frame shifts every later request id (agent_spec asserts exact ids and broke on it)  _(2026-09-02T15:12:44Z)_
- **[command]** headless log/bundle tests must never touch stdpath('log') - log.set_level takes an explicit path as its second argument for exactly that, and the specs point it at vim.fn.tempname()  _(2026-09-02T15:12:44Z)_
- **[invariant]** THE BUNDLE PRINTS WHAT IT NAMES, NEVER WHAT IT IS HANDED. bundle.lua builds the session section and every run-log event's params field by named field, never vim.inspect over a SessionView - that object is a BigSession spread wholesale and carries runner.token (the live control-socket secret), runner.socket, spec, conversation, and a title that IS the first 80 chars of the user's prompt. log.redact on top is belt-and-braces, not the boundary: a redactor cannot catch a field nobody has named yet  _(2026-09-02T15:49:47Z)_
- **[pitfall]** nvime.log writes run in a FAST EVENT CONTEXT (rpc.Client:_dispatch). Nothing below emit() may call vim.fn.* or vim.notify - vim.fn.mkdir there raises E5560 and kills frame dispatch for the whole chunk, forever. The handle is opened in set_level/clear (main loop) and a write that finds none drops its line and schedules one notice. Rotation's os.rename/io.open stay inline on purpose: a deferred rotation cannot bound the file, and plain Lua I/O cannot raise E5560  _(2026-09-02T15:49:47Z)_
- **[pitfall]** vim.system's own opts.timeout does not fire when a grandchild inherits the child's pipes (a shim that spawns sleep) - the on_exit callback never arrives. diagnostics.probe_async carries its own vim.uv timer and sigkills the process; measured, not assumed  _(2026-09-02T15:49:47Z)_
- **[convention]** the debug log is one file per process (nvime-<pid>.log, 0600): two editors sharing one file rotate over each other's history silently. Readers merge every nvime-*.log plus the rotated .1 by the ISO timestamp each line starts with; a clear only ever truncates this process's file  _(2026-09-02T15:49:47Z)_
- **[invariant]** the bundle has THREE paths a secret can take, not two: the session allow-list, the redacted config, and the LOG TAIL it copies verbatim. For that third path redaction must hold AT THE LOG LINE - so anything title-derived is content on both halves (title, branch, slug: a big change's branch is nvime/big/<slug of its title>, and its title is the first 80 chars of the user's prompt), and 'socket' is a SECRET_PART because socket+token is a live control channel. A sidecar event that embeds a branch in prose defeats all of it: carry it as its OWN named field  _(2026-09-02T16:33:28Z)_
- **[pitfall]** vim.system's SystemObj:wait(timeout) answers NIL when the deadline passes - it waits, SIGKILLs, waits again, and hands back an unfilled result. Indexing done.code is how :Nvime doctor crashed on a hung binary, after blocking 2x the timeout. Guard done == nil in any blocking probe  _(2026-09-02T16:33:28Z)_
- **[invariant]** both halves of the debug log stamp the SAME 24-byte prefix (2026-01-01T00:00:00.000Z) because they append to one file and the merged tail sorts on it. The plugin builds it from vim.uv.gettimeofday()+os.date (both fast-context safe; vim.fn.strftime is not). A narrower plugin key made '.' (0x2E) beat 'Z' (0x5A) and hoisted every agent line above every editor line sharing a second  _(2026-09-02T16:33:28Z)_
- **[invariant]** A CONTENT KEY NEVER RECURSES. nvime.log/debuglog.ts summarise a content-named field whatever its type (object -> '<N keys, M bytes>'). The spec leak was the same object one field over from the title: 'spec' WAS a content key, but the summariser only fired for a string/list, so redact walked into BigSpec and goal/approach landed verbatim. 'context' is deliberately NOT a content key - it is a block list in an RPC payload and a settings table in the config, so naming it made one of the two wrong; its children answer for themselves  _(2026-09-02T17:00:05Z)_
