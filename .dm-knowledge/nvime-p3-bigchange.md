- **[invariant]** Claude Agent SDK 0.3.x structured output: options.outputFormat = { type: 'json_schema', schema } and the answer arrives on the RESULT message as result.structured_output (SDKResultSuccess) - never in the assistant text; a turn that cannot satisfy the schema ends with subtype error_max_structured_output_retries  _(2026-08-31T19:43:59Z)_
- **[pitfall]** Parsing a unified diff by scanning for the next @@ / --- splits a hunk whose BODY contains a patch (a test fixture is the ordinary case) - end each hunk by the line counts its @@ header promises instead, and 'No newline at end of file' counts against neither side  _(2026-08-31T19:43:59Z)_
- **[command]** capturing a worktree build: git -C <wt> add -A -N (so files the build created are diffable without a commit) then git -C <wt> diff --no-ext-diff --find-renames <baseCommit> - against the base COMMIT, so a build that committed anyway is still captured; run git with -c core.quotePath=false -c color.ui=false --no-pager or the parse breaks  _(2026-08-31T19:43:59Z)_
- **[pitfall]** The plugin loads agent/dist/index.js, not agent/src - a Lua change against an unbuilt sidecar silently talks to the OLD RPC shape. Always npm --prefix agent run build before any e2e run  _(2026-08-31T19:43:59Z)_
- **[pitfall]** `git worktree add` is NOT a sandbox: the worktree's .git points into the parent repo, so one shell command inside it (`git update-ref`, `gc --prune=now`, `reflog expire`, `push`) rewrites the operator's refs and objects. Use `git clone --local --no-checkout <repo> <dir>` + `remote remove origin` + `checkout --detach <commit>` - hardlinked objects, own ref store, no registration in the source repo  _(2026-09-01)_
- **[pitfall]** `git worktree prune` is repository-global with no expiry: it deletes the admin dir of EVERY registered worktree whose directory is currently missing (unmounted volume, locked home, renamed path), leaving the operator's worktree needing `git worktree repair`. Never run it as setup for an unrelated add  _(2026-09-01)_
- **[invariant]** A diff and the blocks describing it are ONE fact: stage diff.patch, then write diff identity + blocks in a single session.json write. Anything else lets a failure between them render one build's threads over another build's hunks. Hunk ids must be content hashes, not `h<file>.<n>` - a positional id survives a re-capture and silently resolves to a different hunk  _(2026-09-01)_
- **[pitfall]** git appends a literal TAB (plus git's own trailing data) to the `---`/`+++` path when an UNQUOTED path contains a space; `line.slice(4)` keeps it and corrupts the filename. Cut at the first tab unless the path is C-quoted, where the closing quote is the end  _(2026-09-01)_
- **[invariant]** stdpath('data') stores are shared by every open Neovim, so "is anyone driving this" cannot live in one process's Map. Lock file with owner+pid+host+heartbeat; identity is the STORE HANDLE, not the pid, or two handles in one process (and every in-process test) look like the same claimant  _(2026-09-01)_
- **[pitfall]** Any Lua `agent.request` whose handler runs an SDK turn or a full checkout needs `no_deadline`; the default 15s control deadline otherwise abandons it while the sidecar carries on, and the panel keeps a view the sidecar has moved past. Call `M.refresh()` on those error paths  _(2026-09-01)_
- **[pitfall]** A `winbar`/`statusline` evaluates `%{expr}` as vimscript on every redraw. Any user text interpolated into one must have `%` doubled first  _(2026-09-01)_
