- **[command]** tests/e2e/run.sh is the one live e2e entry point (make e2e / make e2e-one SCENARIO=): it builds the sidecar once, gives each scenario its own dir with ALL FOUR XDG homes inside it, bounds it with a per-scenario deadline that kills the whole process group (a detached build outlives its editor and a leaked one keeps spending tokens), and prints a summary table. Never run from CI  _(2026-09-02T20:47:19Z)_
- **[pitfall]** an e2e driver's request params must be vim.empty_dict() when empty, never {} - a bare table encodes as [] and the sidecar refuses it with 'frame.params must be an object'. Also: the big-change store is stdpath('data')/nvime/big (i.e. XDG_DATA_HOME/nvim/nvime/big), NOT XDG_DATA_HOME/nvime - ping's storePath is the chat store and is a different path  _(2026-09-02T20:47:19Z)_
