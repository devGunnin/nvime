- **[command]** tests/e2e/run.sh is the one live e2e entry point (make e2e / make e2e-one SCENARIO=): builds the sidecar once (scenarios skip their own build when NVIME_E2E_BUILT=1), gives each scenario its own dir with all four XDG homes inside it, bounds it with a per-scenario deadline, and prints a summary table. Never run from CI. `run.sh --selftest` proves the runner's own stop paths against stub scenarios and spends no tokens  _(2026-09-02T20:47:19Z, revised 2026-09-03)_
- **[invariant]** KILLING THE PROCESS GROUP IS NOT ENOUGH. A big-change runner is spawned `detached: true` (agent/src/detached.ts), so Node calls setsid(): own session, own process group, reparented to init. It is NOT in the scenario's group and a group kill cannot reach it — and `detached` is the default, so EVERY scenario that builds starts one. The only place its pid is written down is the store (session.json `runner.pid`, lock.json `pid`), which is what tests/e2e/runner-pids.mjs reads. Nothing caps a runner's wall clock, so a leaked one drives the real CLI to completion on its own  _(2026-09-03)_
- **[pitfall]** `set -m` in a runner script puts each scenario in its own process group, which is what makes a group kill possible — and also means a terminal SIGINT never reaches the scenario. Without an explicit INT/TERM trap, Ctrl-C leaves the whole tree running. Related, when testing that trap: a NON-interactive shell starts background jobs with SIGINT already ignored, so a backgrounded run.sh can never see the signal however correct its trap is — the test harness needs `set -m` too, or it proves nothing  _(2026-09-03)_
- **[pitfall]** an e2e driver's request params must be vim.empty_dict() when empty, never {} - a bare table encodes as [] and the sidecar refuses it with 'frame.params must be an object'. Also: the big-change store is stdpath('data')/nvime/big (i.e. XDG_DATA_HOME/nvim/nvime/big) because the plugin passes NVIME_BIG_ROOT, NOT the sidecar's own XDG_DATA_HOME/nvime default - and ping's storePath is the chat store, a third path again  _(2026-09-02T20:47:19Z, revised 2026-09-03)_
- **[pitfall]** a cancel assertion that only waits for the request id to clear is satisfied by a turn that simply FINISHED - completion and cancellation clear it alike. Bound the wait to seconds (a cancel is near-instant, an intake is ~12s) AND check the sidecar's record for what a completed turn would have left behind (an intake leaves a spec). `M.cancel` has TWO nothing-to-stop messages, 'already finished' and 'no big change is running'  _(2026-09-03)_
- **[invariant]** the XDG isolation covers NVIME's own config/data/state/cache, not `~/.claude`. CLAUDE_CONFIG_DIR is where the subscription login lives, so it cannot be redirected: every scenario's prompts and transcripts land in the developer's real claude directory, outside debug-bundle's sentinel grep  _(2026-09-03)_
