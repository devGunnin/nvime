- **[pitfall]** node net.Server.close() on a unix socket unconditionally unlinks the bound path by name, even if another process has since rebound a new socket there — a runner's async close (after its terminal event already streamed) can delete a later bind at the same session's control-socket path; reusing that path must wait for the file to actually disappear first, never rmSync+rebind right after observing the terminal event  _(2026-09-01T14:51:50Z)_
