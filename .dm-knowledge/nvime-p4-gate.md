- **[invariant]** git apply --cached with GIT_INDEX_FILE pointing outside the repo builds a commit (read-tree/apply/write-tree/commit-tree) without touching the operator's index or tree - that is how nvime's local merge keeps exactly ONE mutating command (merge --ff-only) and needs no rollback for anything earlier  _(2026-08-31T22:07:15Z)_
- **[pitfall]** git apply tolerates line-number offsets, so a 'plain fails, --3way succeeds' test needs the operator's change INSIDE the patch's own context window (a change 3+ lines away applies cleanly either way); --3way also needs BOTH blobs in the object db, so a test must commit the reviewed side before rewinding  _(2026-08-31T22:07:15Z)_
- **[pitfall]** captureDiff omits --binary, so a reviewed diff never carries a binary file's bytes and git apply refuses it - the merge refuses binary changes up front rather than landing a patch that cannot apply  _(2026-08-31T22:07:16Z)_
- **[invariant]** blocking paste in a buffer needs three layers, not a keylist: the put mappings, a scoped vim.paste override (what bracketed paste calls - refuse there and the text never reaches the buffer), and nvim_buf_attach on_lines as the catch-all for :put and anything unnamed. Count CHARACTERS (vim.fn.strchars), never #line: 9 CJK chars are 27 bytes and a byte budget locks IME/CJK typists out entirely. It stops a burst, not a determined bypass - say so instead of claiming "no escape hatch"  _(2026-08-31T22:07:16Z)_
- **[invariant]** the base commit is a property of the CHANGE, not of the build clone: reconcile drops the clone when it vanishes but keeps a verifying diff, and a diff with no base cannot be landed - so session.base outlives session.worktree  _(2026-08-31T22:07:16Z)_
- **[command]** live e2e of the release path: build the sidecar, make a scratch git repo, then XDG_DATA_HOME=<scratch> nvim --clean --headless -l <script> driving agent.request(big.create/intake/approve/build/answer/merge) with vim.wait and no_deadline; the real claude CLI grades  _(2026-08-31T22:07:16Z)_
- **[pitfall]** execFileSync(git diff).trim() in a test strips the patch trailing newline and it then fails to apply — append it back  _(2026-08-31T22:07:20Z)_
- **[pitfall]** a build that runs the project's tests leaves artifacts in the clone (__pycache__ etc); anything BINARY among them blocks the merge, since the reviewed diff carries no bytes for it — the refusal names the files, and a repo that gitignores its build output never hits it  _(2026-08-31T22:14:51Z)_
- **[invariant]** git merge --ff-only moves whatever HEAD points at, NOT the branch you name it: re-checking only the base ref lets a checkout made since (a second branch at the same tip, git checkout --detach) fast-forward onto the wrong ref, while every ref the check reads still says what it said. Re-read HEAD - the ref it is ON and the commit it is AT - immediately before the merge and again after  _(2026-08-31T23:02:36Z)_
- **[invariant]** a rollback must be proved against the repository AS THE ATTEMPT FOUND IT (HEAD ref+commit and the git-status lines), never against an absolute "base unmoved and tree clean": status is relative to the NEW HEAD, so a merge that landed on another ref reads as clean, and a tree the operator had already edited reads as a failed rollback  _(2026-08-31T23:02:36Z)_
- **[pitfall]** a SIGKILL of git apply --cached leaves <GIT_INDEX_FILE>.lock beside the private index at every timing; git then refuses every later merge of that session with "another git process seems to be running in this repository" - about a file in nvime store the operator has no reason to know exists. Clear the lock before read-tree and in the same finally as the index  _(2026-08-31T23:02:37Z)_
- **[pitfall]** a long-running agent turn inside a run claim must re-read the record AFTER the turn and mutate that: saving the object read before the turn silently reverts anything written while it ran (the reader pressing X to re-open a thread), which here re-opened the merge gate  _(2026-08-31T23:02:37Z)_
- **[invariant]** a triage turn that rates every thread trivial makes a session mergeable at 0/0 defended with no answer ever typed - and the diff it reads is written by the build agent, so a comment aimed at triage disarms the gate. An all-trivia change gets one extra open thread to acknowledge; no hunks (hides nothing), no signature (asked again after every re-capture), and no setting removes it  _(2026-08-31T23:02:37Z)_
- **[pitfall]** resuming an SDK session AND re-sending the full diff plus every prior answer each round grows context superlinearly and surfaces as an ungraded round nobody can explain. Resume and send only what is new; a thread being graded for the first time still needs its full rendering whatever round it arrives in  _(2026-08-31T23:02:37Z)_
