- **[pitfall]** POSIX puts a ] in the FIRST position of a character class IN the class ([]] is git's literal ]); Lua rejects the resulting empty [] at MATCH time, not build time, so a load-time pcall(string.match,'',pat) probe accepts it and the first real path aborts the whole walk  _(2026-09-01T04:37:17Z)_
- **[pitfall]** FiraCode Nerd Font Mono (both weights) has no braille (U+2800 block), no U+2717 ✗ and no U+2025 ‥ — the usual spinner and cross glyphs render as tofu; nvime.icons sticks to Latin-1/Arrows/Box-Drawing/Block/Geometric only  _(2026-09-01T04:37:17Z)_
- **[pitfall]** vim.fn.strdisplaywidth is not additive across a concatenation on nvim 0.12 once a suite has run: parts summing to 58 measured 64 as one string. Never size a window or assert a layout by measuring rendered lines back — wrap to the width on the way in and trust the line count  _(2026-09-01T04:37:17Z)_
- **[invariant]** Every background nvime paints is a semantic FOREGROUND blended into Normal's bg (palette.blend, ALPHA table) — never a colorscheme's own diff/CursorLine bg, which nvim's default leaves neutral grey. That is what makes the tints work in light and dark alike  _(2026-09-01T04:37:17Z)_
- **[command]** screenshots: tmux new-session -d -x 176 -y 44, drive with send-keys, tmux capture-pane -e -p, then parse the SGR into a cell grid and render with PIL — no browser, no aha/vhs needed  _(2026-09-01T04:37:17Z)_
- **[invariant]** A panel follows its stream from a remembered flag, not by re-comparing the cursor to the line count: one delta can commit several lines at once and a re-derived check unpins the panel for the rest of the run  _(2026-09-01T04:37:17Z)_
