- **[pitfall]** nvim: line_hl_group sits OVER an extmark hl_group foreground, so pinning a line's base colour that way flattens every span on it - apply the base as a full-line hl_group span at a LOW priority instead  _(2026-09-01T13:15:38Z)_
- **[pitfall]** filetype 'markdown' on a rendered scrollback lets vim's own syntax paint UNDER nvime's fg-only extmarks and the attributes merge - markdownStrike/htmlStrike puts a literal line through ~~x~~; panels use filetype 'nvime' (no syntax file) and classify every line themselves  _(2026-09-01T13:15:38Z)_
- **[pitfall]** vim :split copies window-local options from the window it splits, so conceallevel set on the panel scrollback leaks into the prompt below it - set it explicitly on both  _(2026-09-01T13:15:38Z)_
- **[invariant]** Claude Agent SDK 0.3.x with outputFormat json_schema: the turn's text deltas are a draft of the payload and it reports a StructuredOutput tool call - a caller rendering from the PARSED result must suppress both, or the panel shows the working out and then the answer again  _(2026-09-01T13:15:38Z)_
- **[command]** screenshots: tmux capture-pane -e -p STRIPS trailing spaces, so a full-width line_hl_group background renders ragged - pass -N to preserve them before parsing the SGR into a cell grid  _(2026-09-01T13:15:38Z)_
